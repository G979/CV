// import { generalProfile } from './general.profile';
import { frontendProfile } from './frontend.profile';
// import { blockchainProfile } from './blockchain.profile';

// Edit this line to switch which profile is used for the on-screen preview and PDF export.
export const activeProfile = frontendProfile;
